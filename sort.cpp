#include<iostream>
#include<stdlib.h>
int * heapsort(int * arr)
{
    return arr;
}
int main()
{
    int n,*arr;
    arr=(int *)malloc(sizeof(int)*(n+1));
    //size arr[0]
    free(arr);
    return 0;
}